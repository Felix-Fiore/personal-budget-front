import styles from './totalAmount.module.scss';

export const TotalAmount = () => {
    return (
        <div className={styles.amountContainer}>
            <div className="totalCash">$3000</div>
        </div>
    );
};
